var assert =  require('assert');

describe("Asana", function() {
    describe(".connect()", function() {
        it("should connect to Asana API");
    });

    describe(".getUser()", function() {
        it("should get correct user fields");
    });

    describe(".getWorkspaces()", function() {
        it("should get at least one workspaces");
    });

    describe(".getProjects()", function() {
        it("should get at least one project");
    });

    describe(".getTasks()", function() {
        it("should get all task data");
    });
});